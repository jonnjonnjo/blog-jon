{
  "name": "blog-jon",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typst:build": "node scripts/build-posts.mjs",
    "dev": "npm run typst:build && astro dev",
    "build": "npm run typst:build && astro build"
  },
  "dependencies": {
    "astro": "^6.1.9"
  }
}
